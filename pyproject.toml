[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "amd-aux"
version = "1.1.0"
description = "Direct Python bindings for AMD DisplayPort AUX access"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["amd_aux"]
